МЕТОД КАК ЛЮБОЕ КАКОЕ-ТО ДЕЙСТВИЕ ДОЛЖЕН ДАВТЬ КАКОЙТО РЕЗУЛЬТАТ
1)в SessionMasenger перед close session - обязательная проверка на null, т.к.  session.close() returns the connection provided by the application or null;
2)в классе CreditCardActions на 31-32стр.
3)в CreditCardEntity getParameters возвращает лист Parameters по указанным атрибутам,
4)в BasicDao 107 и 109 до одного места
5)что такое CreditCard и почему использую его вместо creditCaedEntity в методе add
6) на 31 CreditCardEntity

7)на 24 CreditCardEntity - маловероятно что из листа первым  вернеться именно баланс
AccessPoint
8)метод нуждающийся в комментариях- гавно-метод
9)генерация id для objects -не верна, для того чтобы сгенерировать уникальное знач, как минимум должны сравнить с предыдущими
10)setBan делать по id - не хорошо
11)после того как админ якобы забанил юзера, юзер с легкостью продолжает пользоваться системой(сonnect не доконца продуман) 
12) разница между fields active and ban, ->setAccountStage() нужен ???
13)два раза засетил один и тот же обьект(розбираю.......)
14)сколько раз подконектился с одного аккаунта в одно и то же время, во столько раз способен создавать количество кошельков;

